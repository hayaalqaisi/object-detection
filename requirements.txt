# Install everything with:  pip install -r requirements.txt
Django>=4.2,<5.1
Pillow>=10.0        # lets Django handle image uploads
numpy>=1.24
tensorflow-cpu>=2.13   # the neural network library (CPU version, no GPU needed)